# ===================================================================
# Application Configuration
# This file contains application runtime configuration only.
# Docker infrastructure (ports, project name, user mapping) is
# configured in compose files.
# ===================================================================

# Riot API Configuration
# NOTE: RIOT_API_KEY is stored in database only (not in .env)
# Set via web UI at /settings or directly in database after startup
# Region/platform: hardcoded to europe/eun1 in backend

# Database Configuration
POSTGRES_DB=riot_api_db
POSTGRES_USER=riot_api_user
POSTGRES_PASSWORD=your_database_password

# Application Configuration
LOG_LEVEL=INFO
JWT_SECRET_KEY=your_jwt_secret_key_here

# Frontend Configuration (Next.js)
# Development: use localhost
# Production: use your public domain (IMPORTANT: must be set during build time for Next.js)
NEXT_PUBLIC_API_URL=http://localhost:8000
NODE_ENV=development

# CORS Configuration
# Development: localhost origins
# Production: Add your production domain (e.g., https://yourdomain.com)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ===================================================================
# Production Deployment Notes:
# - Set NEXT_PUBLIC_API_URL to your production domain (e.g., https://yourdomain.com)
# - Update CORS_ORIGINS to include your production domain
# - Set LOG_LEVEL=INFO for production
# - Use a strong POSTGRES_PASSWORD (generate with: openssl rand -base64 32)
# - Generate strong JWT_SECRET_KEY (run: python -c 'import secrets; print(secrets.token_hex(32))')
# - Set Riot API key via web UI at /settings after first deployment
# ===================================================================
